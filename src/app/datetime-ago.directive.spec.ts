import { DatetimeAgoDirective } from './datetime-ago.directive';

describe('DatetimeAgoDirective', () => {
  it('should create an instance', () => {
    const directive = new DatetimeAgoDirective();
    expect(directive).toBeTruthy();
  });
});
